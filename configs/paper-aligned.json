{
  "schema": "reference_metadata_only_not_consumed_by_cli",
  "warning": "This file separates documented values from implementation choices; it is not a runnable model or training configuration.",
  "acquisition": {
    "matrix_size": [32, 32],
    "wa_center_repetitions": 263,
    "wa_total_repetitions": 68106,
    "ua_analytical_weight": 66.509765625,
    "coherent_sum": true,
    "normalization": "center",
    "normalization_source": "implementation convention; exact paper preprocessing unavailable"
  },
  "model": {
    "provenance": "implementation defaults; widths/depth/heads were not specified in the supplied documents",
    "in_channels": 72,
    "channels": [72, 72, 72, 72],
    "blocks_per_stage": 2,
    "attention_heads": [4, 4, 4, 4],
    "attention_pool_size": [8, 8],
    "query_chunk_size": 1024,
    "temporal_heads": 8,
    "temporal_layers": 2,
    "temporal_mlp_ratio": 4.0,
    "spectral_reduction": 8,
    "norm_groups": 8,
    "dropout": 0.0,
    "residual_output": true
  },
  "training": {
    "provenance": "reported optimizer/schedule plus configurable implementation defaults",
    "epochs": 150,
    "optimizer": "Adam",
    "betas": [0.9, 0.999],
    "loss": "MSE",
    "initial_learning_rate": 0.0003,
    "minimum_learning_rate": 0.000001,
    "scheduler": "CosineAnnealingLR",
    "augmentations": ["horizontal_flip", "vertical_flip", "intensity_scale"],
    "noise_std_bounds": "both CLI bounds are required and must be calibrated by the user; no paper values were supplied"
  },
  "implemented_preprocessing": {
    "network_input_representation": "magnitude_fid",
    "network_output_representation": "real_valued_magnitude_fid_estimate",
    "input_scale_quantile": 0.995,
    "anatomy_normalization": "bilinear resize followed by per-image min-max",
    "anatomy_size": [256, 256],
    "temporal_dynamics": "one global curve per sample and metabolite in the default trainer",
    "spatial_phase": "none in the default trainer",
    "provenance": "explicit implementation fallback; exact study preprocessing was unavailable"
  },
  "simulation": {
    "spectral_points": 72,
    "spectral_bandwidth_hz": 4065.0,
    "implementation_default_peak_offsets_hz": [0.0, -55.0, -209.0],
    "implementation_default_t2_seconds": [0.08, 0.07, 0.06],
    "spectral_defaults_source": "configurable placeholders; exact paper values unavailable"
  },
  "not_specified_in_documents": [
    "model widths/depths/heads",
    "batch size",
    "random seeds and dataset split",
    "augmentation range",
    "exact spectral and phase model",
    "noise calibration and fixed input scaling"
  ]
}
